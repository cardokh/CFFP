# 00 Shared

Shared pipeline material belongs here only when it is reusable across multiple CAutomation pipeline tasks.

Executable scripts must not be placed here directly. Executable units belong under the relevant pipeline `tasks/` folder and must follow the repository scripting infrastructure based on `scripts/shared/base_script.py`.
