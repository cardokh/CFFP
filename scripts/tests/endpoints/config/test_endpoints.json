{
    "baseUrl": "http://127.0.0.1:8000",
    "outputFolder": "scripts/tests/endpoints/output",
    "testSuites": [
        {
            "name": "LLA Existing Endpoints",
            "configFile": "test_endpoints_lla.json"
        },
        {
            "name": "CCore Pipeline CRUD Endpoints",
            "configFile": "test_endpoints_pipelines.json"
        }
    ]
}
