# build_database

Generic PostgreSQL task for the Database Generation stage.

This task consumes validated application database metadata and writes application-specific database output back to the active application folder.
