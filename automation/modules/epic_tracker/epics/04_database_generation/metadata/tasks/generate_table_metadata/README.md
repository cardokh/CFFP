# generate_table_metadata

Generic metadata task for the Database Generation stage.

This task reads from and writes to the active application folder. It must not store application-specific metadata inside the generic database-generation module.
