# Engine Stages

Engine stages contain reusable automation logic.

Each numbered stage maps to the same numbered stage under each application.

The stage folders define what the engine can do. They do not own application data.
