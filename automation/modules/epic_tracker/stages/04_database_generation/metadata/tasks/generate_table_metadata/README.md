# Generate Table Metadata

Generic reusable task for the Database Generation stage.

This task reads from and writes to the active application folder. It must not store application-specific artifacts inside the generic stage folder.
