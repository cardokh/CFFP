# CAutomation Input Contracts

Canonical hierarchy:

- `project/contracts/` contains project-level CAutomation platform contracts.
- `modules/<module_id>/contracts/` contains module-level contracts.

Task 02 must normalize both levels while preserving this distinction.
